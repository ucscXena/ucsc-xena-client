# disable test, until we can run headless, or with phantom, or something.
test:
    override:
        - "true"
deployment:
    dev:
        branch: master
        commands:
            - npm run build:prod
            - rsync -a --delete --rsync-path='sudo rsync' build/ ubuntu@ec2-52-91-68-9.compute-1.amazonaws.com:/var/www/html
